---
phase: 06-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .goreleaser.yaml
  - README.md
autonomous: true
requirements: [FOUN-01]
must_haves:
  truths:
    - "Release packaging config exists for target platforms."
    - "Project docs include hardening/release commands."
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "multi-arch release config"
    - path: "README.md"
      provides: "operator hardening instructions"
---

<objective>
Finalize release packaging and docs so snapshot/release workflows are reproducible.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add GoReleaser v2 configuration</name>
  <files>.goreleaser.yaml</files>
  <action>Define CGO-disabled multi-arch build matrix and archive/checksum configuration.</action>
  <verify>Config is syntactically valid YAML and matches build targets.</verify>
  <done>Project is ready for `goreleaser release --snapshot` usage.</done>
</task>
<task type="auto">
  <name>Task 2: Document hardening commands</name>
  <files>README.md</files>
  <action>Add hardening checks section with size gate + test command and release config mention.</action>
  <verify>README includes reproducible hardening steps.</verify>
  <done>Release prep guidance is documented.</done>
</task>
<task type="auto">
  <name>Task 3: Final phase verification</name>
  <files>README.md</files>
  <action>Run full tests and static build to verify no regressions from hardening additions.</action>
  <verify>`go test ./...` and static linux build succeed.</verify>
  <done>Phase 6 changes are stable and validated.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/06-hardening/06-02-SUMMARY.md`
</output>
